package:
  name: mpifft4py
  version: "1.0"

source:
  git_url: ../../

build:
  number: 16

requirements:
  build:
    - toolchain
    - python
    - cython
    - numpy

  run:
    - python
    - numpy
    - scipy
    - mpi4py
    - cython
    - pyfftw

test:
  requires:
    - pytest

  imports:
    - mpiFFT4py
