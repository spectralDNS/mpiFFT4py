package:
  name: mpifft4py
  version: "1.0"
  
source:
  git_url: ../../

build:
  number: 14

requirements:
  build:
    - python
    - cython
    - numpy

  run:
    - python
    - numpy
    - scipy
    - mpi4py
    - cython

test:
    requires:
      - pytest

