package:
  name: mpifft4py
  version: "1.0"
  
source:
  git_url: ../../

build:
  number: 10

requirements:
  build:
    - python
    - cython
    - numpy
    - gcc

  run:
    - python
    - numpy
    - scipy
    - mpi4py
    - cython

test:
    requires:
      - pytest

